type Player {
  id: ID!
  name: String!
  lastname: String!
  birthDate: Date
  positions: [Position!]!
  phoneNumber: String
  email: String
  photo: String
  createdAt: Date
  updatedAt: Date
}

type Query {
  Player(id: ID!): Player!
  allPlayers: [Player]!
}
